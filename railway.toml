[build]
builder = "nixpacks"
buildCommand = "npm install"
startCommand = "npm start"

[deploy]
restartPolicyMaxRetries = 3
NODE_ENV = "production"

[build.ignore]
paths = [
  "frontend/**",
  "node_modules/**",
  ".git",
  ".gitignore",
  "README.md",
  "*.md",
  ".env*"
]
