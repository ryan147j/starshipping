# ==== Database config ====
DB_NAME=starshipping
DB_USER=root
DB_PASSWORD=root
DB_HOST=localhost
DB_DIALECT=mysql

# ==== Server port ====
PORT=5000

# ==== Email / SMTP configuration for contact & booking ====
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=webstarshipping@gmail.com          # Gmail account that will SEND the emails
SMTP_PASS=yruqwbdfnpedicoi                  # Gmail App Password
SMTP_FROM="StarShipping Team <webstarshipping@gmail.com>"

# Where contact messages should be delivered
CONTACT_RECIPIENT=contact1@starshipping.com.tn

# Where booking requests should be delivered
BOOKING_RECIPIENT=documentation@starshipping.com.tn

# ==== Chatbot (Mistral API) ====
MISTRAL_API_KEY=bX8QCbXKhBeFVhoAWxEsv2WsdqARyb2P

# ==== HTTPS & Security ====
# Enable HTTPS in local development when certs exist in ../certs
DEV_ENABLE_HTTPS=true
BACKEND_HTTPS_KEY=../certs/localhost-key.pem
BACKEND_HTTPS_CERT=../certs/localhost.pem

# In production behind a reverse proxy, set to true to force HTTPS redirects
# and set FRONTEND_URL to your HTTPS site (e.g. https://www.starshipping.com.tn)
ENFORCE_HTTPS=false
FRONTEND_URL=https://localhost:5173